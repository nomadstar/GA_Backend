# Example environment for analytics
# Path to the sqlite file used by the analytics subsystem.
# If not set, defaults to "analithics/analytics.db"
ANALITHICS_DB_PATH=analithics/analytics.db

# Alternatively, specify the DB via URL. Examples:
# SQLite (file):
# ANALITHICS_DB_URL=sqlite:///home/usuario/quickshift/analithics/analytics.db
# or relative file:
# ANALITHICS_DB_URL=sqlite://analithics/analytics.db

# Postgres remote (example):
# ANALITHICS_DB_URL=postgres://user:password@db.example.com:5432/mi_analytics_db

# You can also set an absolute path or a relative one for ANALITHICS_DB_PATH.
# ANALITHICS_DB_PATH=/var/lib/quickshift/analytics.db
